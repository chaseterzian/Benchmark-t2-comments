<%= form_for @music do |f| %>
<% if @music.errors.any? %>
  <ul>
    <% @music.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>
<% end %>

     <!--Lines 1-9 - This is the second batch of code which relates to the validations 
         in the "music.rb" model file.  It is the second batch of code needed
         to make the validations work properly so that you can get errors
         on the edit page in the browser after hitting the submit button. The code
         in the "new.html.erb" file between lines 1 and 9 is (in this case)
         identical to the code in this file-->


<%= form_for @music do |f| %>
<br>
<%= f.label :song %>
<%= f.text_field :song %>
<br>
<br>
<%= f.label :artist %>
<%= f.text_field :artist %>
<br>
<br>
<%= f.submit "Submit edit" %>
<hr>
<%= link_to "Meh, remove this bitch", music_path(@music),
data: {confirm: "Are you sure you want to remove this bitch?"}, method: "delete" %>
|
<%= link_to "Back to playlist", root_path %>
<% end %>
